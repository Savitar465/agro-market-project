export enum ProductStatus {
  /** Visible in the public catalog and purchasable. */
  Published = 'PUBLISHED',
  /** Hidden from the public catalog (kept by the seller, not deleted). */
  Suspended = 'SUSPENDED',
}
